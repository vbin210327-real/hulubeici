export default function Home() {
  return (
    <main>
      <h1>葫芦背词 API</h1>
      <p>
        This Next.js application backs the Hulu Beici vocabulary app. Use the REST endpoints under
        <code> /api </code> to sync custom wordbooks, progress, and user settings. See the repository
        README for setup instructions.
      </p>
    </main>
  );
}
